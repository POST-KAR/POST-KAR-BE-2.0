server.port=${SERVER_PORT:8080}

# ===================== MongoDB Config =====================
spring.data.mongodb.database=${MONGODB_DATABASE:postkardb}
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://postkar:Qwertzxc111@cluster0.gchm1d1.mongodb.net/postkardb?retryWrites=true&w=majority&appName=Cluster0}
spring.data.mongodb.auto-index-creation=true

# ===================== Task Execution Config =====================
spring.task.execution.pool.core-size=4
spring.task.execution.pool.max-size=8

# ===================== JWT Config =====================
app.jwt.secret=${JWT_SECRET:2fTAbpeYgx7eBs8QBqygQL7zPVKaD6RB}
app.jwt.access-token-validity=${JWT_ACCESS_TOKEN_VALIDITY:86400000}
app.jwt.refresh-token-validity=${JWT_REFRESH_TOKEN_VALIDITY:604800000}

# ===================== Logging =====================
logging.level.org.springframework=${LOGGING_LEVEL_SPRING:INFO}
logging.level.com.ag.project=${LOGGING_LEVEL_PROJECT:DEBUG}

# ===================== CORS Config =====================
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}
app.cors.allowed-methods=${CORS_ALLOWED_METHODS:GET,POST,PUT,DELETE,OPTIONS}
app.cors.allowed-headers=${CORS_ALLOWED_HEADERS:*}
app.cors.allow-credentials=${CORS_ALLOW_CREDENTIALS:false}

# Application name
spring.application.name=${APPLICATION_NAME:post-kar}

server.forward-headers-strategy=native

# File upload limits
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# AWS S3 configuration
aws.s3.region=${AWS_S3_REGION:ap-south-1}
aws.s3.bucket=${AWS_S3_BUCKET:postkar}
aws.access.key=${AWS_ACCESS_KEY:AKIA552GHUZT27ONFTID}
aws.secret.key=${AWS_SECRET_KEY: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}

# App-specific settings
signed.url.expiry.hours=24
app.signed-url-expiry-minutes=1440
app.current-version=v1

spring.main.allow-circular-references=true
