# ===================== Active Profile =====================
# Default profile is prod. To switch environments:
# - Dev: --spring.profiles.active=dev or set SPRING_PROFILES_ACTIVE=dev
# - Stage: --spring.profiles.active=stage or set SPRING_PROFILES_ACTIVE=stage
# - Prod: --spring.profiles.active=prod or set SPRING_PROFILES_ACTIVE=prod
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

server.port=${SERVER_PORT:8080}

# ===================== MongoDB Config =====================
spring.data.mongodb.database=${MONGODB_DATABASE:postkardb}
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://postkar:Qwertzxc111@cluster0.gchm1d1.mongodb.net/postkardb?retryWrites=true&w=majority&appName=Cluster0}
spring.data.mongodb.auto-index-creation=true

# ===================== Task Execution Config =====================
spring.task.execution.pool.core-size=4
spring.task.execution.pool.max-size=8

# ===================== JWT Config =====================
app.jwt.secret=${JWT_SECRET:2fTAbpeYgx7eBs8QBqygQL7zPVKaD6RB}
app.jwt.access-token-validity=${JWT_ACCESS_TOKEN_VALIDITY:86400000}
app.jwt.refresh-token-validity=${JWT_REFRESH_TOKEN_VALIDITY:604800000}

# ===================== Logging =====================
logging.level.org.springframework=${LOGGING_LEVEL_SPRING:INFO}
logging.level.com.ag.project=${LOGGING_LEVEL_PROJECT:DEBUG}

# ===================== CORS Config =====================
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}
app.cors.allowed-methods=${CORS_ALLOWED_METHODS:*}
app.cors.allowed-headers=${CORS_ALLOWED_HEADERS:*}
app.cors.allow-credentials=${CORS_ALLOW_CREDENTIALS:false}

# ===================== Email Configuration =====================
# Configure SMTP settings for your email provider
# For Gmail: host=smtp.gmail.com, port=587, use app password
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:arpangautam299@gmail.com}
spring.mail.password=${SMTP_PASSWORD:qtfc tjms ixdo bhfq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=${MAIL_FROM:POST-kAR}

# Application name
spring.application.name=postkar

server.forward-headers-strategy=native

# File upload limits
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# Cloudflare R2 Storage Configuration
cloudflare.r2.account-id=${CLOUDFLARE_R2_ACCOUNT_ID:28d7f56b3627d31aca6c85d8a06c2519}
cloudflare.r2.access-key-id=${CLOUDFLARE_R2_ACCESS_KEY_ID:bf412d810d989d024370fcc86b7c98bc}
cloudflare.r2.secret-access-key=${CLOUDFLARE_R2_SECRET_ACCESS_KEY:b97c4083108470c3c28a8a9253d76434680efda0b1b913ecbfc2c99df878611c}

# App-specific settings
signed.url.expiry.hours=24
app.signed-url-expiry-minutes=1440
app.current-version=v1

spring.main.allow-circular-references=true
spring.http.client.factory=simple

